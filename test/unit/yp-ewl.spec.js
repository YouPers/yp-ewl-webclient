'use strict';

/* jasmine specs for controllers go here */

describe('yp-ewl app', function(){
//  beforeEach(module('yp-ewl'));


  it('should do something', inject(function() {
    //spec body
  }));

  it('should so something', inject(function() {
    //spec body
  }));
});
