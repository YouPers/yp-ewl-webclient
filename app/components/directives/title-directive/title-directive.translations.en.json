{
  "pageTitle": {
    "default": "YOUPERS HealthCampaign {{ campaign.topic.name }}",
    "signin": {
      "content": "Sign In - YOUPERS HealthCampaign"
    },
    "account": "Account",
    "profile": "Profile",
    "dcm": {
      "home": "YOUPERS HealthCampaign {{ campaign.topic.name }}",
      "activity": "{{ activity.title }}",
      "ideas": "Ideas for {{ campaign.title }}",
      "recommendation": "{{idea.title}}",
      "topics": "Choose Program",
      "campaign": "{{ (campaign.topic.name || newTopic.name) | uppercase}} edit"
    },
    "dhc": {
      "game": "Home",
      "activity": "{{activity.title}}",
      "check": "Self-assessment"
    }
  }
}